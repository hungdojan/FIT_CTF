version: '3.8'

name: {{ name }}

services:
  admin:
    build:
      context: ./admin
      dockerfile: Containerfile
    networks:
      - {{ name }}_main_net
  http:
    build:
      contenxt: ./http
      dockerfile: Containerfile
    networks:
      - {{ name }}_main_net

networks:
  {{ name }}_main_net:
    name: "{{ name}}_main_net"
